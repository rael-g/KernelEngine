// ke.samplers.slang — engine-owned sampler objects.
// Authors reference these by name; never create custom samplers in material code.

SamplerState    ke_samp_linear;
SamplerState    ke_samp_nearest;
SamplerState    ke_samp_aniso;
SamplerState    ke_samp_shadow;
SamplerState    ke_samp_point_clamp;

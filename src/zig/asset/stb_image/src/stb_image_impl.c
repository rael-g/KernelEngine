/* stb_image is header-only; this translation unit compiles the implementation.
 * Kept out of @cImport because translate-c cannot lower stb's JPEG decoder. */
#define STB_IMAGE_IMPLEMENTATION
#include <stb_image.h>

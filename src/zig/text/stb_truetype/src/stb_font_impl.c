/* stb_truetype is header-only; this translation unit compiles the
 * implementation. Kept out of @cImport because translate-c cannot lower
 * stb's internals. */
#define STB_TRUETYPE_IMPLEMENTATION
#include <stb_truetype.h>

namespace Pong;

/// <summary>Play-area constants shared by Paddle (clamp) and Ball (scoring zone).</summary>
public static class Field
{
    public const float HalfW         = 8f;
    public const float HalfH         = 4.5f;
    public const float WallThickness = 0.25f;
}

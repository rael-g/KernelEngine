<Project>
  <!--
    Shared helper for locating native build outputs, compiled into every
    sample so each one does not carry its own path arithmetic.
  -->
  <ItemGroup>
    <Compile Include="$(MSBuildThisFileDirectory)ExamplePaths.cs"
             Link="ExamplePaths.cs" />
  </ItemGroup>
</Project>
